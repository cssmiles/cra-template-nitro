import { Header } from './components';

function App() {
  return (
    <div>
      <Header />
      <p>
        Scaffolding project with eslint rules, husky hooks, prettier config,
        vscode recommended plugins, test utils, skeleton folder structure and
        some reasonable defaults.
      </p>
    </div>
  );
}

export default App;
